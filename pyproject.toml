[project]
name = "hubspot-segmentation-bot"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "flexus-client-kit",
    "httpx",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["flexus_my_bots*"]
